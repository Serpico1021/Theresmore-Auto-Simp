# Codemap Conventions — theresmore

`codemap regen` 目前只能扫到 6 个文件、7 个符号，几乎全部来自根目录几个巨型 `.user.js` 里
最外层的 `NumberParser` 类。原因已确认：`automation-src/fragments/smart-build-planner.js`
的核心逻辑整体包在一个立即执行函数 `const smartBuildPlanner = (() => { ... })()` 里，
所有函数都缩进了一层，codemap 的提取器只认列宽 0 的顶层声明，因此拿不到里面任何符号。
这不是 bug 也不打算改代码结构去迁就工具，而是手工维护这份文件作为符号索引的补充，
**每次改动 fragments 后同步更新本文件，并跑一次 `codemap regen --repo theresmore`**。

## automation-src/fragments/smart-build-planner.js（853 行，IIFE 包裹，2026-08-25 先后因
"自由思想家学院"奇观组件 bug 修复、Research 目标驱动模块新增，行号已两次整体下移，
以下为最新准确行号）

顶层数据表（57–251 行，未受本次修复影响）：
- `smartBuildGoals` 57 — 5 个具名目标（resourceFocus/buildingFocus/targetTechs/weights）
- `smartBuildRoutes` 123 — 各目标的建筑数量路线表
- `smartBuildDangerousFights` 206 — 危险科技 → 需要先打赢的战斗
- `smartBuildBattleTemplates` 216 — 危险战斗的推荐兵种/建筑模板

`smartBuildPlanner` IIFE 内部函数（253–786 行，均为 `const fn = (...) => {}`）：
- 目标/路线读取：getOptions 254, getGoal 277, getRoute 278, getRouteTargets 279,
  **expandPrerequisiteTargets 283**（新增，路线/goal.buildingFocus 共用的"递归展开建筑
  前置条件"逻辑，从原 getExpandedRouteTargets 里抽出来的共享辅助函数）,
  getExpandedRouteTargets 316（改写为调用 expandPrerequisiteTargets）,
  **getExpandedGoalFocusTargets 320**（新增，对 goal.buildingFocus 做同样的前置递归展开，
  修复"本体在 buildingFocus 里但隐藏前置组件建筑不在里面导致永不被造"的问题——例如
  academy_of_freethinkers 本体在 buildingFocus 里，但它的前置 academy_of_freethinkers_part
  组件建筑不在里面，之前永远不会被自动排队建造）,
  getRouteEntry 330, getGoalTechs 331, getGoalDangerousFights 332
- 状态查询：getResourceMap 258, getCount 266, getUnitCount 271, hasIndexedOrRunItem 336,
  isTechCompleted 351, isUnlockCompleted 354, isDangerousResearchStructurallyReady 360
- 危险战斗闸门：getNextDangerousFight 371, canWinDangerousFight 381,
  getBlockedDangerousFights 392, **shouldGateDangerousResearch 408**（对外暴露）
- 资源缺口/加分计算：getResourceCost 415, registerResourceCapShortfall 420,
  getResearchResourceCapShortfalls 436, getRouteBuildingResourceCapShortfalls 449,
  getResourceCapShortfalls 466, getGoalResourceBonus 480, getGoalRequirementBonus 490,
  getCapShortfallBonus 508, getBattleTemplate 519, getTemplateBuildingEntry 523,
  getDangerousBattleBuildingBonus 524, getRouteRequirementBonus 537,
  **getGoalFocusPrerequisiteBonus 544**（新增，配合 getExpandedGoalFocusTargets 给
  buildingFocus 的隐藏前置建筑打分，公式与 getRouteRequirementBonus 一致：未达标时
  `80 + (target-count)*14 + priority*5`，已达标时按 priority 给 3 或 8 的低分维持），
  getCostWait 552, getBottleneckScore 564, getCapPressure 580
- 建筑评分主函数：**scoreBuilding 589**（核心打分入口，622 行 `score += getGoalFocusPrerequisiteBonus(building, goal);`
  为新增调用点）, toPriority 628, toExtra 636
- 目标产出组装：applyManualOverrides 641, applyRouteTargets 651,
  applyCapBridgeTargets 668, applyDangerousBattleBuildingTargets 686
- 兵种侧：getTemplateUnitEntry 705, getUnitScore 706, applyUnitManualOverrides 720,
  **getUnitTargets 730**（对外暴露）
- **Research 打分侧（2026-08-25 新增，goal-driven 升级第一个子系统）**：
  getResearchGroups 760（`groupChoices(tech)` 的薄封装，自己重新分组而不依赖 base 脚本里
  已被 `.pop()` 消费过的 `researchGroups`/`groupedResearchs` 变量，避免耦合求值顺序）,
  getTechUnlockBonus 761（反查 `buildings` 里 `req: {type:'tech', id: 该科技}` 的建筑，
  若该建筑在 `getExpandedGoalFocusTargets(goal)` 里则按其 priority 加分——科技侧复用了
  之前给 academy_of_freethinkers_part 做的前置展开逻辑，不需要再为科技单独维护"解锁了哪个
  目标建筑"的手动表）,
  getResearchProductionBonus 772（按 `smartBuildStrategyWeights[strategy].science` 和
  `goal.resourceFocus` 给 `tech.gen` 里的资源产出/军队 modifier 打分）,
  **scoreResearch 785**（科技打分主函数，关键安全默认：`resetResearch`——即 base 脚本里
  `launch_annhilator` 这类终局/不可逆科技——除非在 `goal.targetTechs` 里显式包含，否则
  强制打 0 分排除；`options.researchExcludes` 作为额外的手动强制排除兜底；`goal.targetTechs`
  命中给 +220 高分；危险科技（`tech.confirm` 或 base 脚本 `unsafeResearch` 列表）只加一个
  基础分，真正的"能不能打赢就不研究"闸门仍由已有的 `shouldGateDangerousResearch`/
  base 脚本 `dangerousFightsMapping` 在执行时把关，scoreResearch 不重复做安全判断）,
  applyResearchManualOverrides 797（与 applyManualOverrides 同款语义：`manualOverrides`
  开启时，用户在 Exclusive/Regular/Dangerous 面板里手动设的非零优先级覆盖智能算出的值）,
  **getResearchTargets 804**（对外暴露，返回 `{科技id: 优先级(0-7，复用 toPriority 的分桶)}`
  扁平表——与 base 脚本 `getAllowedResearch()` 消费的 `state.options...RESEARCH.options`
  形状完全一致；内部对每个"互斥科技组"（`groupChoices(tech)` 分组）只保留分数最高的
  一个成员非零，其余强制清零，模拟 UI 里 `<select>` 只能选一个的语义）
- 建筑侧总入口：**getTargets 823**（对外暴露，内部依次调用上面的 apply* 系列）
- 导出：`return { getTargets, getUnitTargets, getResearchTargets, shouldGateDangerousResearch }` 844

## automation-src/fragments/smart-build-options.js
单个 `smartBuild` 默认配置对象（约 17 行，含 2026-08-25 新增的 `researchEnabled`/
`researchExcludes`），字段不多，直接读全文比查符号快，不单独维护索引。

## automation-src/fragments/smart-build-panel.template.html
smartBuild 自身开关面板（56 行），注入到 base 模板第 50069 行附近（Cheats 页签内）。

## automation-src/base/*.base.user.js（5 万+ 行单体模板，构建产物外壳）
不是日常维护目标，AGENTS.md 明确禁止手改，只能改 fragments 后用 build.ps1 重新生成。
已确认的关键锚点（新增子系统时来这里插一行调用 planner，不要在附近大改结构）：

- Build 建筑注入点：`const buildingsObject = smartBuildPlanner.getTargets(subpage, configuredBuildingsObject) || configuredBuildingsObject;` — 约 47312–47313 行
- Build 手动配置 UI（大列表，8 类）：模板字符串约 49482–49529 行；smartBuild 开启且未勾
  manualOverrides 时完全不生效，仅作 fallback / manualOverrides 输入源
- Army 兵种数据读取点：约 46870–46871 行；Army 兵种手动配置 UI：约 49670–49775 行
  （`getUnitTargets` 只有在存在被阻挡的危险科技战斗时才接管，多数时间手动列表仍生效，
  不要误以为这块已经被智能化）
- Research 候选科技读取点 `getAllowedResearch()`：约 47879–47899 行（**2026-08-25 已接入
  planner**：`const researchOptions = smartBuildPlanner.getResearchTargets(configuredResearchOptions)
  || configuredResearchOptions;`，与 Build/Army 注入点同款写法——**这是本项目 CLAUDE.md
  "禁止手改 base 模板"规则生效后第一次经用户明确同意的例外**，后续新增子系统若要同样接入
  base 模板，仍需先向用户确认，不能援引此例外自行下手）；
  Research 手动配置 UI（Exclusive/Regular/Dangerous 三区块）：约 49530–49595 行，
  `manualOverrides` 开启时仍作为覆盖智能优先级的输入源；
  `resetResearch`（终局/不可逆科技，约 47875 行）、`unsafeResearch`（危险科技，约 49383 行）、
  `groupChoices`（互斥科技分组函数，约 46835 行）这三个 base 脚本里的既有变量/函数被
  smart-build-planner.js 直接按名引用（同一顶层作用域，只在游戏循环调用时才求值，
  声明顺序不影响正确性，不需要额外传参）
- Research 执行/点击循环 `executeAction$3()`（含终局科技 `resetResearch` 特殊处理）：约
  47860–48053 行区间
- Population 职业列表：49636–49650 行；Magic：49791–49871 行；Diplomacy：49876–49912 行；
  Prestige：49996–50013 行 —— 均未接入 planner，均是未来待做子系统

维护完这份文件后跑一次 `codemap regen --repo theresmore` 确认合并进 CODEMAP.md。
