# Codemap Conventions — theresmore

`codemap regen` 对本项目根目录几个巨型 `.user.js`（构建产物、base 模板）符号提取有限——
这些文件要么是压缩/巨型单体脚本，要么内部逻辑整体包在运行时才拼接进去的 IIFE 里，
提取器只认列宽 0 的顶层声明，覆盖不到内部函数。这不是 bug，也不打算为了迁就工具去改
产物/base 模板结构，而是手工维护这份文件作为符号索引的补充：**每次改动
`automation-src/fragments/` 后同步更新本文件，并跑一次 `codemap regen --repo theresmore`**。

## 2026-08-26：smart-build-planner 从单文件重构为分层多文件（重要架构变更）

原 `automation-src/fragments/smart-build-planner.js`（853+ 行，全部内容整体包在
`const smartBuildPlanner = (() => { ... })()` 一个 IIFE 里，函数统一缩进一层，codemap
完全提取不到符号）已拆分为 **`automation-src/fragments/smart-build-planner/` 目录下 10 个
按"层次"分类的文件**（2026-08-26 新增 `80-prayer-scoring.js` 后从 9 个变为 10 个），
取代原单文件。设计动机：修复 academy_of_freethinkers_part bug 时
发现 codemap 对这坨代码基本失明，且状态读取逻辑（`idxs.buildings` 键名/含义）容易被
反复猜错——拆分后每个文件职责单一、列宽 0（codemap 能正常提取），且把"读游戏状态"
的代码收拢到唯一一个 adapter 文件，避免以后每个子系统各自重新踩键名坑。

**拼接机制**：这些文件不是真正的 ES module，彼此没有 import/export。`build.ps1` 按固定
顺序清单读取 `00-data-tables.js` + `10~90` 八个文件，把 `10~90` 整体拼接后包进
`const smartBuildPlanner = (() => { ...拼接内容... })();`（`00-data-tables.js` 保持在
IIFE 外部，和原来一样），最终产物文本结构与拆分前等价。文件内函数一律写在列宽 0（不带
额外缩进），因为包裹 IIFE 是 build.ps1 拼接时加的字符串，不写进文件本身——**这些文件单独
存在时不是合法 JS（`90-export.js` 是裸 `return` 语句），这是设计如此，不是缺陷**。
跨文件引用（比如 `60-research-scoring.js` 用 `20-goal-routes.js` 的 `getGoalTechs`，
或用 `40-build-scoring.js` 的 `toPriority`）合法，因为最终都在同一个 IIFE 作用域内。

### 文件清单与职责（`automation-src/fragments/smart-build-planner/`）

- **`00-data-tables.js`**（在 IIFE 外部）：纯数据表，无逻辑。
  `smartBuildDefaults`、`smartBuildResources`、`smartBuildStrategyWeights`、
  `smartBuildGoals`（5 个具名目标：progress/moonlightNight/druid/gloriousRetirement/
  annihilator）、`smartBuildRoutes`（各目标的建筑数量路线表）、`smartBuildDangerousFights`
  （危险科技→需要先打赢的战斗）、`smartBuildBattleTemplates`（危险战斗的推荐兵种/建筑模板）、
  **`smartBuildTitanOverrides`**（2026-08-26 新增，存储上限驱动的建筑数量规划子系统的一部分——
  `{泰坦建筑id: {replaces: [{id, capFactor}], goals?: [...]}}`，由 `40-build-scoring.js`
  的 `applyTitanOverrides` 消费：泰坦建筑**已建成**（`getCount>=1`，不是"已解锁"）且
  `goals` 未指定或命中当前 goal 时，把 `replaces` 里普通建筑的目标数量按 `capFactor`
  缩减/清零。**首版是空表**，具体游戏内泰坦建筑 id/替代关系待后续单独确认后填充，
  见 `docs/superpowers/specs/2026-08-26-storage-driven-building-cap-design.md` 的
  "开放问题"）。

- **`10-game-state-adapter.js`**（50 行）——**游戏状态适配器，全项目里唯一允许直接碰
  `reactUtil.getGameData()`/`state.options`/`resources` 的地方**，其余文件一律通过这里
  暴露的函数读状态，不直接查 `idxs`/`run`：
  - `getOptions`、`getResourceMap`
  - `getCount(building)` ——**2026-08-26 依据游戏源码彻底重写**（见下方"关键排查记录"），
    正确读法是 `gameData.idxs.buildings[building.id]`（**原始 id，永远不带 `bui_` 前缀**）
    拿到的是这个建筑在 `gameData.run.buildings` 数组里的**下标**，不是数量本身；真正数量是
    `gameData.run.buildings[idx].value`。之前两轮修复（查带前缀 key、查不带前缀 key 当数值用、
    线性 `find` 兜底）全部基于错误假设，已废弃。
  - `getUnitCount(unit)` ——同款 idx→数组下标模式，读 `gameData.idxs.army[unit.id]` +
    `gameData.run.army[idx].value`（尚未像 getCount 一样对照游戏源码逐行核实，行为目前正常，
    如果以后某个兵种也出现"数量对不上"，先来这里重新核实，不要凭空猜）。
  - `hasIndexedOrRunItem`、`isTechCompleted`、`isUnlockCompleted`
  - **`isBuildingUnlocked(building)`**（2026-08-26 新增，通用版"结构性是否可建造"检查，
    抽取自 `30-dangerous-fight-gate.js` 的 `isDangerousResearchStructurallyReady` 同款
    req 遍历逻辑）：忽略 `resource` 类型的 req（只看是否卡在建筑前置/科技/信仰/传承解锁上），
    `building` 类型 req 查 `getCount(prereq) >= req.value`，其余类型丢给 `isUnlockCompleted`。
    供 `40-build-scoring.js` 的 `applyCapBridgeTargets` 过滤"存储缺口候选建筑"，避免把还没解锁的
    仓储建筑当成能立刻补足缺口的方案。
  - **`getCurrentStageIndex()`**（2026-08-27 新增，见下方"阶段驱动的建筑数量自动计算引擎"
    条目）：遍历全部 `buildings`，取 `hasStageGate` 为真、`isBuildingUnlocked` 为真且
    `age!==100` 的建筑里 `age` 最大值作为当前阶段，供 `40-build-scoring.js` 的
    `getStageCap` 使用。
  - **`hasStageGate(building)`**（2026-08-27 新增，修复实机反馈的回归，见下方条目末尾）：
    `req` 里是否存在非 `resource` 类型的真实门槛（`tech`/`building`/`prayer` 等）——纯资源
    花费的建筑（如 `gan_eden`/`research_plant`）不算"达到该阶段"的信号，否则从开局起就会
    把阶段误判过高。

- **`20-goal-routes.js`**（59 行）：目标/路线解析，build+research+危险战斗闸门共用。
  `getGoal`、`getRoute`、`getRouteTargets`、**`expandPrerequisiteTargets`**（路线/
  `goal.buildingFocus` 共用的递归展开建筑前置条件逻辑）、`getExpandedRouteTargets`、
  **`getExpandedGoalFocusTargets`**（对 `goal.buildingFocus` 做同样的前置递归展开，修复
  "本体在 buildingFocus 里但隐藏前置组件建筑不在里面导致永不被造"的问题——
  academy_of_freethinkers 的前置 academy_of_freethinkers_part 就是这个场景）、
  `getRouteEntry`、`getGoalTechs`、`getGoalDangerousFights`。

- **`30-dangerous-fight-gate.js`**（约54行）：危险战斗闸门。
  `isDangerousResearchStructurallyReady`、`getNextDangerousFight`、`dangerousFightCache`
  （2.5s 缓存，避免每帧重算）、`canWinDangerousFight`、`getBlockedDangerousFights`（仍然
  只看当前 goal 链条上"下一个"危险科技，用于兵种囤积优先级）、
  **`shouldGateDangerousResearch`**（对外暴露，供 base 模板 Research 执行循环调用；
  2026-08-26 修正：不再局限于"当前 goal.targetTechs 链条上排到的那一个"危险科技，
  只要是 `smartBuildDangerousFights` 映射表里的全部8个科技，只要 smartBuild 开着就一律
  要求 base 脚本的 `armyCalculator.canWinBattle` 检查通过才允许点击研究——修复此前
  `orc_horde`/`dragon_assault`/`mysterious_robbery`/`fallen_angel`/`kobold_nation`/
  `barbarian_tribes`/`mindless_evil` 这7个在非对应 goal 下完全没有 canWinBattle 保护的
  安全缺口。`orcish_threat`/`huge_cave_t` 不在这张映射表里，不受此函数影响，继续靠
  `60-research-scoring.js` 的基础分不提权来兜底。
  **2026-08-26（第四次）追加修复——不再要求 `options.armyEnabled`**：见下方"巨龙来袭
  自动点击导致死亡重置"条目，`armyEnabled`（军队自动化/招募目标开关）和"点危险科技前
  要不要先模拟战斗"是两件不该耦合的事，用户关掉前者会连带关掉这条安全闸门本身，
  已解耦，现在只要 `options.enabled`（smartBuild 总开关）为真就一律生效）。

- **`40-build-scoring.js`**：建筑打分主逻辑，全项目最大的一块。
  资源缺口/加分：`getResourceCost`、`registerResourceCapShortfall`、
  `getResearchResourceCapShortfalls`、`getRouteBuildingResourceCapShortfalls`、
  `getResourceCapShortfalls`、`getGoalResourceBonus`、`getGoalRequirementBonus`、
  `getCapShortfallBonus`、`getBattleTemplate`、`getTemplateBuildingEntry`、
  `getDangerousBattleBuildingBonus`、`getRouteRequirementBonus`、
  **`getGoalFocusPrerequisiteBonus`**（配合 `getExpandedGoalFocusTargets` 打分：
  未达标 `80+(target-count)*14+priority*5`，已达标按 priority 给 3 或 8 的低分维持；
  2026-08-26 追加修复——不再对"直接写在 `goal.buildingFocus` 里的本体"短路排除，本体和
  它的隐藏前置现在共用同一套强加分公式，见下方"refugee_district_part 造好后难民区本体
  仍不自动建造"条目）、`getCostWait`、`getBottleneckScore`、
  `getCapPressure`。打分主函数：**`scoreBuilding`**、`toPriority`（分桶到优先级
  0/3/4/5/6/7，被 `60-research-scoring.js`/`80-prayer-scoring.js` 跨文件复用）、`toExtra`。
  **`STAGE_DECAY_FLOOR`/`getStageCap(building, options)`**（2026-08-27 新增，见下方
  "阶段驱动的建筑数量自动计算引擎"条目，被 `60-research-scoring.js`/`80-prayer-scoring.js`
  跨文件复用）——取代已删除的 `getPlannedTarget`/`GENERAL_SCORE_PLAN_CAP_MULTIPLIER`。

  **2026-08-26 新增：存储上限驱动的建筑数量规划**（设计 spec：
  `docs/superpowers/specs/2026-08-26-storage-driven-building-cap-design.md`）：
  - `PRODUCTION_STORAGE_CAP_SECONDS`（常量，默认 90，不开放 UI 配置）、
    **`getProductionStorageCap(building, resourceMap, options)`**——边际填满时间阈值法：
    对建筑每个 `gen.type==='resource'&&value>0` 的产出项，算"再建一个后的净速率"
    `res.speed + item.value`，若 `<=0`（净速率仍非正）不设上限；否则算
    `(res.max-res.current)/projectedSpeed` 的填满秒数，低于阈值就把该建筑当前已建数量
    `getCount(building)` 作为硬上限（多种产出资源取最严格的那个）；都不触发则返回
    `Infinity`。**只用当前实时 `res.max`/`res.current`/`res.speed`，不对未来仓储建筑
    做预测**——规划器每轮重调用，`res.max` 随仓储建筑建成自然变大，上限自动跟着松动。
  - **`applyTitanOverrides(targets, options)`**——消费 `00-data-tables.js` 的
    `smartBuildTitanOverrides`，泰坦已建成（`getCount>=1`）且 goal 匹配时按
    `capFactor` 缩减/清零 `replaces` 里普通建筑的目标数量。
  - `applyCapBridgeTargets` 的候选建筑过滤新增 `isBuildingUnlocked(building)`
    （`10-game-state-adapter.js`）——只有当前已可建造的建筑才会被当成"能补足存储缺口"
    的方案，不会把目标寄望在还没解锁的仓储建筑上。`getCapShortfallBonus`（评分阶段的
    软性加分）不受影响，仍对所有匹配建筑独立加分——锁定建筑加分不会被下游点击逻辑
    实际使用，不构成问题。

  目标产出组装：`applyManualOverrides`、`applyRouteTargets`、`applyCapBridgeTargets`、
  `applyDangerousBattleBuildingTargets`、`applyTitanOverrides`、**`getTargets`**
  （对外暴露，建筑侧总入口）——`getTargets` 内 `max` 计算已接入
  `getProductionStorageCap`（`Math.min` 链新增一项），`options.maxTarget`（默认 80）
  仍保留但降级为兜底上限，多数场景先被存储阈值封顶；`route`/危险战斗备战/存储缺口桥接
  这三条"强制推荐"路径**不接入**存储上限，优先级高于泛化的产出过剩判断。执行顺序：
  各建筑打分 → `applyCapBridgeTargets` → `applyDangerousBattleBuildingTargets` →
  `applyRouteTargets` → `applyTitanOverrides` → `applyManualOverrides`（手动配置最终
  覆盖权不变）。

  **（已废弃，2026-08-27 替换）** 2026-08-26 曾新增过通用打分循环的"计划倍数"封顶
  （`GENERAL_SCORE_PLAN_CAP_MULTIPLIER`+`getPlannedTarget`），用于临时缓解"农场之类
  产出建筑无限乱建"的问题。这套机制已被 2026-08-27 的"阶段驱动的建筑数量自动计算引擎"
  整体取代并删除（见下方"2026-08-27：阶段驱动的建筑数量自动计算引擎"条目），`getTargets`
  的 `max` 计算链现在用 `getStageCap(building, options)` 这一项，不再有
  `GENERAL_SCORE_PLAN_CAP_MULTIPLIER`/`getPlannedTarget` 这两个符号。

- **`50-unit-scoring.js`**：兵种侧。`getTemplateUnitEntry`、`getUnitScore`、
  `applyUnitManualOverrides`、**`getUnitTargets`**（对外暴露）。
  - 2026-08-26 新增：**探索单位保底招募**（解决"军队探索从不自动触发"的根因——
    `getExploreTargets`〔`70-explore-scoring.js`〕只在探索单位存量>0 时才给出非零
    派遣目标，但过去唯一的自动招募入口 `getUnitTargets` 只在 `blockedFights` 非空
    时生效，且 `getUnitScore` 对非战斗单位恒为 0 分会被过滤掉，导致 scout/explorer/
    familiar 永远招不到、存量永远是 0）。新增 `smartBuildExploreBaselineExcludedGoals`
    （首版只有 `'moonlightNight'`——自动月明之夜自杀重置目标下不生效）、
    `EXPLORE_BASELINE_COUNT`（=3，写死不开放 UI）、`getExploreBaselineTargets(options)`
    （复用 `70-explore-scoring.js` 的 `smartBuildExploreUnits` 表拿 unitId，目标数量
    `min(unit.cap || 3, 3)`，优先级固定 `prio=4`，独立于 `blockedFights`）。
  - `getUnitTargets` 结构随之改变：不再是"没有 `blockedFights` 就整体 `return null`"，
    而是 `targets = getExploreBaselineTargets(options)` 打底，`blockedFights` 非空时
    再把战斗单位目标（`prio` 通常 8~10）合并进同一个 `targets`；合并后仍为空对象才
    `return null`（兜底退回手动配置的语义不变）。即：没有危险战斗需要备战时，军队页面
    也不再完全是手动列表说了算——探索单位保底招募会生效；Attack/常规战斗自动化仍是
    未来待做子系统，这一点没变。
    设计 spec：`docs/superpowers/specs/2026-08-26-explore-baseline-recruitment-design.md`。

- **`60-research-scoring.js`**（73 行）：Research 打分侧（2026-08-25 新增的第一个
  goal-driven 子系统）。`getResearchGroups`（`groupChoices(tech)` 的薄封装，自己重新
  分组而不依赖 base 脚本里已被 `.pop()` 消费过的变量）、`getTechUnlockBonus`（反查
  `buildings` 里 `req:{type:'tech',id:该科技}` 的建筑，若该建筑在
  `[...getExpandedGoalFocusTargets(goal), ...getExpandedRouteTargets(route)]` 里且未达标则按其
  priority 加分——**2026-08-26 补充 route 参数 + `getCount(building) >= entry.target` 已达标
  跳过判断**，此前只看 goal.buildingFocus、且不管建筑是否已建好都持续加分，与
  `getPrayerUnlockBonus`〔`80-prayer-scoring.js`〕的写法对齐）、
  **`getPrayerTechBonus`**（2026-08-26 新增，修复"难民区组件卡住"问题的关键一环——
  `refugee_district_part` 除了建筑前置外还要求 `{type:'prayer', id:'the_aid'}`，而
  `the_aid` 祈祷本身又要求 `{type:'tech', id:'the_scourge'}`；此前 `getTechUnlockBonus`
  只反查"科技解锁了哪个建筑"，反查不到"科技解锁了哪个祈祷"，导致
  `the_scourge` 拿不到任何隐藏前置加分、链条在这一跳断掉。`getPrayerTechBonus` 反查
  `spells` 里 `type==='prayer' && req:{type:'tech',id:该科技}` 的祈祷，直接复用
  `getPrayerUnlockBonus(prayer, goal, route)`〔同一 IIFE 作用域内跨文件调用〕算出该祈祷
  本身是否被需要及其加分，两者共用同一套"60+priority*8"打分惯例，无需重新定义量纲）、
  `getResearchProductionBonus`、**`scoreResearch`**（签名新增 `route` 参数；关键安全默认：`resetResearch`
  终局/不可逆科技除非在 `goal.targetTechs` 里显式包含否则强制 0 分排除；
  `options.researchExcludes` 手动兜底排除；`goal.targetTechs` 命中 +220；
  2026-08-26 修正——`isDangerous = technology.confirm || unsafeResearch.includes(id)`：
  非危险科技基础分 4→8（解决"当前 goal 没有具体目标科技时打分永远卡在
  `toPriority(4)=0`、导致类似 chemistry 这种游戏本身可选但自动化永远不选"的问题，
  chemistry 案例：`progress` goal 下无 targetTechs 命中、无 gen 字段产出加分、
  唯一解锁建筑 alchemic_laboratory 属于 druid.buildingFocus 不算本goal加分，
  故长期卡在基础分 4）；危险科技（`isDangerous`）基础分**保持 4 不变**，不吃这次提权，
  只加"是否被挡住"的 40/10 加分——真正"能不能打赢就不研究"的闸门在
  `30-dangerous-fight-gate.js` 的 `shouldGateDangerousResearch`/base 脚本执行时把关，
  这里不重复判断、也刻意不通过提高基础分去间接增加危险科技被选中的概率）、
  `applyResearchManualOverrides`、**`getResearchTargets`**（对外暴露，
  内部对每个互斥科技组`groupChoices(tech)`只保留分数最高的一个成员非零）。

- **`70-explore-scoring.js`**（44 行）：Explore 打分侧（2026-08-26 新增的第二个
  goal-driven 子系统，Army 阶段的第一部分——只做探索 min/max 阈值，不含 Attack 自动化，
  按用户明确的范围划定）。`smartBuildExploreUnits`（三个纯探索兵种：scout/explorer/
  familiar——它们没有战斗风险，但每次探索有资源花费，早期这个花费重要、后期几乎无影响，
  4 号侦察单位 `drone_u` 未接入 base 脚本手动配置 UI，本次不管）、
  `getExploreAffordabilityFactor`（按 `unit.reqAttack` 里资源类花费的当前富余程度算
  0.15~1 的节流系数，复用 `resourceMap` 的 fillRatio/speed）、`applyExploreManualOverrides`、
  **`getExploreTargets`**（对外暴露，返回 `{scoutsMin/scoutsMax/explorersMin/...}` 扁平表，
  与 base 脚本 Explore 页面 `state.options...EXPLORE.options` 形状一致）。

- **`80-prayer-scoring.js`**（31 行）：Prayer 打分侧（2026-08-26 新增的第三个
  goal-driven 子系统，解决"Progress 目标卡在魔法-祈祷-解决危机"的架构性缺口——
  Magic/Prayers 子页面此前从未接入 planner，`getAllowedPrayers()` 在默认空配置下恒返回
  `[]`，页面自动化实质上从未运行）。结构与 `60-research-scoring.js` 完全同构：
  **`getPrayerUnlockBonus(prayer, goal, route)`**（反查 `buildings` 里
  `req:{type:'prayer',id:该祈祷}` 的建筑，若该建筑在 `getExpandedGoalFocusTargets(goal)`
  或 `getExpandedRouteTargets(route)` 里且未达标，按对应 `priority` 给 `60+priority*8`
  加成——跟 `getTechUnlockBonus` 方向相反：那边是"科技解锁了建筑"，这里是"建筑依赖了
  祈祷"）、**`scorePrayer`**（基础分 10，`toPriority(10)=3`，确保无加成祈祷长期也会被
  自动祈祷掉；`options.prayerExcludes` 手动兜底排除给 0 分）、`applyPrayerManualOverrides`、
  **`getPrayerTargets`**（对外暴露，`options.prayerEnabled===false` 时返回 `null` 退回
  手动配置；输出扁平 `{prayerId: priority}` 形状，跟 `getResearchTargets` 一致——
  `spells.filter(spell => spell.type === 'prayer')` 只取祈祷类条目，`type:'spell'`
  的法力系统条目不在范围内）。设计 spec：
  `docs/superpowers/specs/2026-08-26-prayer-auto-planner-design.md`。
  **开放风险**（未逐行扫描 4368 行的 `spells` 数据表确认）：是否存在危险/不可逆祈祷分支
  需要类似 Research `resetResearch`/`confirm` 的阻断机制，抽样未发现但非全表确认，
  留待实机验证。

- **`90-export.js`**（8 行）：`return { getTargets, getUnitTargets, getResearchTargets,
  getExploreTargets, getPrayerTargets, shouldGateDangerousResearch };` —— IIFE 收尾，
  单独文件不是合法 JS（设计如此）。

### 关键排查记录：academy_of_freethinkers_part 不自动建造 bug（2026-08-25~26）

完整过程见 git 历史/会话记录，这里只留结论供以后类似"游戏状态读不对"问题参考：

- **真正原因**：`getCount` 原实现用 `keyGen.building.key(id)`（即 `bui_` + id）去查
  `idxs.buildings`，但根据 `files/theresmore/main.js`（游戏本体 F12 Sources 导出，见下方
  "游戏源码参考"）第 49986-50003 行 `addBuilding()` 的真实实现，`idxs.buildings` 的 key
  **永远是不带前缀的原始 building id**，且**值不是数量，是这个建筑在 `run.buildings`
  数组里的下标**，真正数量在 `run.buildings[idx].value`。`bui_` 前缀只是翻译/i18n 字符串表
  的 key 命名习惯（如 `bui_academy_of_freethinkers_part: "自由思想家学院组件"`），跟
  `idxs.buildings` 完全是两套命名空间。
- 原 bug 对**所有建筑**都存在，只是大多数建筑"读到 0"不影响智能规划器的行为（无论如何都会
  重新排队建造），而 academy_of_freethinkers_part 是"consume:true 型奇观组件"，被这个 bug
  锁死在低分/低 max，永远进不了点击队列。
- 排查中间踩过的坑（已废弃，不要重蹈）：查不带前缀的 `idxs.buildings[id]` 当作"数量"用——
  实际读到的是数组下标（比如 49），远超建筑 cap（12），导致打分逻辑把它当成"已严重超建"
  疯狂扣分，表现为优先级恒为 0、日志完全不触发，反而更难排查。
- 现在的正确实现见上方 `10-game-state-adapter.js` 的 `getCount` 条目。

### 2026-08-26：refugee_district（难民区）卡在 refugee_district_part 不自动建造

现象：`progress` goal 下自动脚本一直不建造"难民区组件"（`refugee_district_part`），
而难民区（`refugee_district`）是用户判断后续"巨龙来袭"分支需要的前置奇观。根因和
academy_of_freethinkers_part 那次不同，是两个独立问题叠加：

1. **`refugee_district` 压根不在 `progress` goal 的 `buildingFocus` 里**（`00-data-tables.js`
   `smartBuildGoals.progress`，此前只有 `city_center`/`academy_of_freethinkers`/
   `mana_pit`/`harbor_district`），也没有 `smartBuildRoutes.progress` 路线表——
   `refugee_district` 只在 `druid`/`annihilator` 两个 goal 里被目标化过。经用户确认后
   已把 `'refugee_district'` 加入 `progress.buildingFocus`，让通用的
   `expandPrerequisiteTargets`（`20-goal-routes.js`）机制自动展开出
   `refugee_district_part`（`req:{type:'building',id:'refugee_district_part',value:8,
   consume:true}`）——这一步复用的是 academy 那次已验证过的通用机制，不是重新造轮子。
2. **`refugee_district_part` 除了资源还要求 `{type:'prayer',id:'the_aid'}`，`the_aid`
   又要求 `{type:'tech',id:'the_scourge'}`**——这是"建筑→祈祷→科技"三跳前置链，比
   academy 单纯的"建筑→建筑"两跳更长一环。`80-prayer-scoring.js` 的
   `getPrayerUnlockBonus` 已经处理了"建筑→祈祷"这一跳，但 Research 侧
   `getTechUnlockBonus` 只反查"科技解锁了哪个建筑"，反查不到"科技解锁了哪个祈祷"，
   `the_scourge` 因此拿不到任何隐藏前置加分。新增 `getPrayerTechBonus`
   （`60-research-scoring.js`）补上这一跳，见上方该文件条目。
   **经验教训**：以后再遇到"某个目标建筑迟迟不自动建造"，先顺着它的完整 `req` 链条
   （建筑 `main.js` 里的定义）走一遍，确认每一跳（建筑→建筑、建筑→祈祷、祈祷→科技……）
   是否都有对应的反查加分机制打通，链条断在哪一跳就补哪一跳，而不是默认只有
   academy 那种"建筑→建筑"模式需要特殊处理——**奇观类建筑的"主体+consume:true组件"
   结构本身是通用机制已覆盖的，真正容易漏掉的是组件后面还挂着的非 building 类型前置
   （prayer/tech 等）反查加分**。

### 2026-08-26（续）：refugee_district_part 造好后，难民区本体仍不自动建造

上一节修复的是"组件（`_part`）造不出来"；这次是组件造够 8 个之后**本体
（`refugee_district`）依然不进点击队列**，两个是链条上前后两跳的独立 bug。

根因在 `40-build-scoring.js` 的 `getGoalFocusPrerequisiteBonus`：该函数原本开头有
`if (goal.buildingFocus && goal.buildingFocus.includes(building.id)) return 0;`——
对"直接写在 `goal.buildingFocus` 里的本体"（如 `refugee_district`）主动跳过强加分，
理由是"本体已经被 `getGoalRequirementBonus` 里的 `goal.buildingFocus.includes(...)  ? 34 : 0`
加过分了，不要重复计分"。但这个**扁平 +34** 和 `getGoalFocusPrerequisiteBonus` 给
"隐藏前置"（比如 `refugee_district_part`）用的公式 `80+(target-count)*14+priority*5`
（对 refugee_district_part：target=8, priority=7 → 80+8*14+35=207，`toPriority`直接封顶 7）
量级完全不对等——本体是奇观（`cat:'wonders'`），`scoreBuilding` 里还会先扣 28 分
（`risk!=='aggressive'`时）、再乘 `strategyWeights.wonders`（balanced=0.35），
一堆折损后只剩个位数分数，全靠这 +34 兜底，经常连 `toPriority` 的最低档（score>=7→3）
都摸不到，导致组件造够后本体没有对应的强加分把它推进点击队列——表现和
academy_of_freethinkers_part 那次几乎一样，都是"链条断在某一跳没有强加分"，只是这次
断的不是 prerequisite 反查缺失，而是"本体自己被显式排除在强加分公式之外"。

**修复**：删掉 `getGoalFocusPrerequisiteBonus` 里那行排除本体的 `if` 短路。
`getExpandedGoalFocusTargets` 展开时本来就会把种子（`goal.buildingFocus` 里的本体）
自己也放进 `byId`（`target:1, priority:6, reason:'goal building focus'`），所以本体
和它的隐藏前置现在走同一套公式：未达标 `80+(1-0)*14+6*5=124`，已达标（`count>=target`）
按 `priority>=8?8:3` 给低分维持——和已有的 `getRouteRequirementBonus`/prerequisite 分支
惯例完全对齐，不是新发明的量纲。`getGoalRequirementBonus` 里那个扁平 +34 没有删，两者
叠加只是让本体分数更高，无副作用。这个改动对所有 `buildingFocus` 里的本体都生效（不只
refugee_district），理论上也会让 city_center/academy_of_freethinkers/mana_pit/
harbor_district 这类本体在达成前置后被推进的力度比之前更强。
**尚未实机验证**（无法在对话里跑游戏确认点击发生），下次遇到类似"目标建筑迟迟不建"
问题先看这两个函数有没有类似的"排除本体"短路。

### 2026-08-26（第三次）：难民区仍不自动建造——真正根因是存储上限把目标钉死在 0

上面两次修复（补 buildingFocus 加分 + 移除本体的强加分短路）都没解决问题。用户提供了
`files/20260826/Options.txt`（`smartBuild` 配置导出）和存档导出文件，用嵌入在生成产物里的
`LZString.decompressFromBase64`（脚本自带，见约 49930-50410 行）解出来实机核对：
`smartBuild.enabled=true`、`goal='progress'`、`manualOverrides=false`——配置侧没有问题；
存档里 `refugee_district_part` 已建满 8/8，`the_scourge` 科技、`the_aid` 祈祷均已完成
（`value:1`），`refugee_district` 本体 `idxs.buildings['refugee_district']` 是
`undefined`（游戏侧从未创建这个建筑的 run 条目，即 0 个）——**游戏侧结构性前置条件全部满足**，
问题 100% 在规划器这一侧。

真正根因：`40-build-scoring.js` 的 `getProductionStorageCap`（2026-08-26 早些时候新增的
"存储上限驱动的建筑数量规划"，见 `docs/superpowers/specs/2026-08-26-storage-driven-building-cap-design.md`）
在判定"资源已过剩"时，把上限设成 `Math.min(cap, getCount(building))`——**对于从未建过的建筑
（`getCount===0`），这个上限直接是 0**，而这一项和 `getGoalFocusPrerequisiteBonus`
的强加分是完全独立的两条链路（前者算 `max`，后者算 `score`/`prio`），分数再高也没用，
`getTargets` 里 `max<=count`（`0<=0`）直接 `return`，连 target 都不会被写进去。
`refugee_district.gen` 里有 `building_material`/`supplies`/`steel` 三个小额（各 0.2/秒）
正向产出项，只要其中任意一个资源当前净速率为正且"填满剩余仓容所需时间 < 90 秒"（后期这三个
资源通常又便宜又有大量仓储建筑，非常容易触发），这个判定就会命中，把从未建过的
`refugee_district` 永久锁在 0——**这个设计缺陷不是 refugee_district 专属，任何"当前一个都没有、
但产出了某个已经快装满仓库的资源"的建筑，第一次都造不出来，规划器再怎么加分都没用**，
是比前两次的"加分链条断裂"更底层的一类 bug：**`max` 计算链路和 `score`/`prio` 计算链路是分开的
两条独立逻辑，改分数只能影响后者，前者要单独排查**。

**修复**：`getProductionStorageCap` 里 `return Math.min(cap, getCount(building));` 改成
`return Math.min(cap, Math.max(getCount(building), 1));`——含义从"不再多建"改成
"不强行超过当前数量，但至少留出建第一个的空间"，符合设计初衷（阻止已经建过的产出建筑继续
无脑叠加）又不会误伤"一个都还没有"的建筑。这是通用修复，不是只给 refugee_district 开特例。
设计 spec 的"开放问题"里补一条待办：`getCount(building)===0` 时不该把上限钉死为 0，
以后再碰到"目标建筑分数很高但就是不建"的情况，除了查 `score`/`getGoalFocusPrerequisiteBonus`
这条链路，还要单独查 `max` 计算链路（`getTargets` 里 `Math.min` 那五项，尤其是
`getProductionStorageCap`），两条链路都要过一遍，不能只查其中一条就下结论。
**尚未实机验证**（无法直接读取 `resources.get()` 的实时 DOM 数据确认这就是本次触发的具体
资源，但代码逻辑本身的缺陷是确定的，且和现象完全吻合）。

**排查方法记录**：`Options.txt`/存档导出文件是 `LZString.compressToBase64(JSON.stringify(...))`
编码的（生成产物里对应 `document.querySelector('#...-save').value = LZString.compressToBase64(...)`
和 `LZString.decompressFromBase64(saveString)`，约 51463/51471 行）。解码时把生成产物里
49930-50410 行的 LZString 实现整段抠出来存成独立 `.js` 文件（标准 lz-string v1.4.5，`module.exports`
可用），`node` 里 `require` 后调用 `decompressFromBase64` 即可还原成 JSON 字符串。存档 JSON 顶层是
`{'0':版本号字符串,'1':idxs对象,'2':run对象,...}`——`'1'`是 `idxs`（`caps/resources/buildings/
population/techs/modifiers/army/prayers` 各是"id→run数组下标"的映射），`'2'`是 `run`（实际游戏状态，
`race/resources/buildings/techs/prayers/...`），和 `10-game-state-adapter.js` 里
`reactUtil.getGameData().idxs`/`.run` 的结构完全对应，可以据此直接核对任意建筑/科技/祈祷的
真实数量而不用靠猜。但 `resources.get(id)`（`getResourceMap` 用的那个）是**纯 DOM 抓取**
（读 `#root` 里渲染出来的资源表格文本），不在存档 JSON 里，没法从存档反推，只能在实机里
用控制台核实或让用户在游戏里直接看资源面板。

### 2026-08-26（第四次）：巨龙来袭（dragon_assault）自动点击导致死亡重置——armyEnabled 意外关掉了危险战斗闸门

现象：用户反馈脚本自动点击了"巨龙来袭"科技研究，但没有准备驻防军队，导致战斗失败、直接触发
`LegacyStore.toggleSoftReset` 死亡重置。

排查过程（本次先排除了一个看起来合理但实际不成立的假设，记录下来避免以后重蹈）：

1. **一开始怀疑"时序竞态"**：`executeAction$3`（Research 执行循环，base 模板约
   47925-48034 行）在确认 `canWinNow`（当前可用驻防兵力能赢）后会把 `state.stopAttacks`
   置为 `false` 再点击研究，怀疑 Attack 自动出击（`executeAction$6`）会在两次动作之间
   插进来把刚确认能赢的驻防部队派出去。**已排除**：`mainLoop`（base 模板约
   50628-50696 行）是严格单线程串行调度，`pagesToCheck` 一次只 `await` 一个页面的
   `action()` 完整跑完才轮到下一个，Attack 和 Research 不可能在同一次点击流程中交替执行。
2. **确认战斗是同步结算，不是延迟事件**：对照游戏本体源码 `files/theresmore/main.js`
   43489 行 `processBoss(e,t)` → `processDefense(e)` → `_fight('defense',e)`，确认
   confirm 弹窗点掉的同一瞬间就用真实战斗引擎同步结算胜负（不是文案"接下来的几个小时"
   暗示的延迟事件），所以闸门检查（`canWinBattle`）和真实结算之间理论上没有时间窗口。
3. **真正根因**：经用户确认，事发时 Research 页面手动"危险战斗"勾选框是**关闭**的，且用
   同一份 `files/20260826/Options.txt`（第三次排查用过的那份导出，用户确认就是事发前
   在用的配置）解码核对，`smartBuild.armyEnabled: false`（军队自动化开关关着，其余
   `enabled:true/goal:'progress'/researchEnabled:true` 均正常）。而
   `shouldGateDangerousResearch`（`30-dangerous-fight-gate.js`）原实现是
   `if (!options.enabled || !options.armyEnabled || !smartBuildDangerousFights[researchKey]) return false;`
   ——**armyEnabled=false 直接让这条函数对所有危险科技都返回 false**，等于自动闸门和
   手动勾选框同时失效，`dragon_assault` 被当成普通科技一样点了研究，`canWinBattle`
   模拟战斗压根没跑过。

**设计缺陷**：`armyEnabled` 语义上是"军队自动化（自动招募/组队目标）开关"，用户关掉它是
想自己手动管理兵种配置，不代表"不需要危险科技点击前的安全检查"——这是两件不该耦合的事，
前者是"要不要帮你决定招什么兵"，后者是"点一个不可逆的危险操作前要不要先确认打得过"，
后者理应默认安全、不受前者影响。

**修复**：`shouldGateDangerousResearch` 去掉 `!options.armyEnabled` 这个条件，改成只要
`options.enabled`（smartBuild 总开关）为真就生效，与 `armyEnabled` 解耦。
`getBlockedDangerousFights`（同文件，供兵种囤积优先级用）**保持不变**仍然依赖
`armyEnabled`——这个函数管的是"要不要帮用户自动调整招兵目标去囤兵"，用户关掉军队自动化时
不应该被这条覆盖，语义上和安全闸门是两回事，不能一并解耦。

**尚未实机验证**（死亡已重置，无法复现同一次事件），但根因链路（armyEnabled=false → 
`shouldGateDangerousResearch` 恒 false → 手动勾选框也关着 → 两条防线同时失效 → 
`dragon_assault` 被当普通科技点击 → `canWinBattle` 从未被调用）逻辑自洽，且和用户描述
的"没有准备驻防军队就直接死亡"完全吻合。以后再排查"危险科技/危险战斗为什么没被拦下"，
先查 `options.enabled`/`options.armyEnabled`/Research 页面手动勾选框这三个开关的实际值，
不要默认闸门函数本身在跑。

### 2026-08-27：阶段驱动的建筑数量自动计算引擎（替代 smartBuildRoutes 手写数字）

设计 spec：`docs/superpowers/specs/2026-08-27-stage-driven-building-cap-engine-design.md`。
背景：`moonlightNight` goal 下 `common_house` 卡在 10 个不涨（base 模板 47434-47438 行硬编码
"超过10个必须食物净速率>1"的安全阀，配合路线表里 `farm` 目标只给 5 个食物跟不上）、
`lumberjack_camp`/`sawmill` 等没进路线表的生产建筑无限乱建（`getPlannedTarget` 对它们恒返回
`null`，`Math.min` 链退化成 `Infinity`，只剩 `maxTarget=80` 兜底）。

**核心信号**：已用 Grep 核实游戏数据自带 `age` 字段（小整数 1/2/3/4/5/13，或哨兵值 100）和
`cap` 字段（奇观类单体建筑如 `city_center` 显式 `cap:1`），不需要新建里程碑科技表：
- `getCurrentStageIndex()`（**新增于 `10-game-state-adapter.js`**）：遍历全部 `buildings`，
  取 `isBuildingUnlocked` 为真且 `age!==100` 的建筑里 `age` 的最大值作为当前阶段 `S`；
  无解锁建筑时默认 `S=1`。
- `getStageCap(building, options)`（**新增于 `40-build-scoring.js`**，紧邻
  `getProductionStorageCap` 之后）：`T = building.age===100 ? S : building.age`；
  `gap = S-T`；`gap<=0` 返回 `cap`（`building.cap||options.maxTarget`）；`gap>0` 返回
  `Math.max(STAGE_DECAY_FLOOR=4, Math.round(cap / 2**gap))`（几何衰减，每差1世代对半，
  不收紧到0）。`building.cap` 天然处理了奇观单体建筑（`cap:1` 恒是最紧约束），不需要
  额外区分"单体 vs 可堆叠"。

**数据表精简**：`00-data-tables.js` 的 `smartBuildRoutes[goal].buildingTargets/
supportTargets` 每个条目**删除了 `target` 数字字段**，只保留 `id`/`priority`/`reason`——
路线表从"造多少个"降级为纯定性数据（这个 goal 需要哪些建筑、多紧急）。
`smartBuildGoals[goal].buildingFocus`（结构性前置，如 `refugee_district_part` 的
`target:8` 来自 `req.value`）**不受影响**，那是游戏规则硬性数量，不是本次替代对象。

**接入点变化**：
- `getTargets`（`40-build-scoring.js`）的 `max` 计算 `Math.min` 链，`getPlannedTarget`+
  `GENERAL_SCORE_PLAN_CAP_MULTIPLIER`（上一轮存储上限设计引入的临时补丁）**整体删除**，
  换成 `getStageCap(building, options)` 这一项，对通用打分循环里的**所有建筑统一生效**，
  不区分是否在路线表里——这是"统一覆盖漏网建筑"的关键，`lumberjack_camp`/`sawmill` 不需要
  手工加路线表条目就能获得合理上限。
- `getTargets` 循环开头新增 `routeTargetIds`（`getExpandedRouteTargets(route)` 的 id 集合，
  循环外算一次，不在每个建筑迭代里重算），`if (!prio) return;` 改成
  `if (!prio && !routeTargetIds.has(building.id)) return;`——避免路线里列出的建筑因打分侧
  0 分被提前跳过导致连 `max` 都算不出来。
- `applyRouteTargets`（`40-build-scoring.js`）大幅简化：不再计算/写入独立的 `routeMax`，
  只在 `building.id in targets` 时 `Math.max` 提升 `prio_${id}`——数量上限完全交给通用循环
  的 `getStageCap`。
- `getRouteRequirementBonus`（打分侧加分，`40-build-scoring.js`）、
  `getRouteBuildingResourceCapShortfalls`（存储缺口候选，`40-build-scoring.js`）原本读
  `routeEntry.target` 做"是否达标"判断，**改为读 `getStageCap(building, options)`**，
  两处都新增了 `options` 参数并沿调用链（`getCapShortfallBonus`→`getResourceCapShortfalls`）
  一路透传。
- **跨文件回归修复**：`getTechUnlockBonus`（`60-research-scoring.js`）、
  `getPrayerUnlockBonus`（`80-prayer-scoring.js`）之前把
  `getExpandedGoalFocusTargets(goal)` 和 `getExpandedRouteTargets(route)` 合并成一个数组
  统一用 `entry.target` 判断"这个建筑还需不需要更多"——路线条目删掉 `target` 字段后，
  `expandPrerequisiteTargets`（`20-goal-routes.js:16`）里 `Number(entry.target)||1` 的
  默认值会让路线来源的条目全部退化成 `target:1`，导致研究/祈祷侧"这个建筑还需要更多，对应
  科技/祈祷该提权"的判断在建成 1 个后就失效（明显回归，不是本次设计的一部分，是精简路线表
  数字字段时顺带发现并修的连锁问题）。修复：两个函数分别拆开 `focusTargets`/`routeTargets`
  两个数组、新增 `options` 参数，路线来源条目改用 `getStageCap(building, options)` 判断达标，
  `buildingFocus` 来源条目继续用原有的结构性 `entry.target`。

**影响文件**：`00-data-tables.js`（`smartBuildRoutes` 删 `target` 字段）、
`10-game-state-adapter.js`（新增 `getCurrentStageIndex`）、`40-build-scoring.js`（新增
`STAGE_DECAY_FLOOR`/`getStageCap`，删 `getPlannedTarget`/`GENERAL_SCORE_PLAN_CAP_MULTIPLIER`，
改 `getTargets`/`applyRouteTargets`/`getRouteRequirementBonus`/
`getRouteBuildingResourceCapShortfalls`/`getResourceCapShortfalls`/`getCapShortfallBonus`/
`applyCapBridgeTargets` 的 `options` 透传）、`60-research-scoring.js`（改
`getTechUnlockBonus`/`getPrayerTechBonus`/`scoreResearch` 调用处）、`80-prayer-scoring.js`
（改 `getPrayerUnlockBonus`/`scorePrayer` 调用处）。已跑 `build.ps1` + `node --check` +
`codemap regen --repo theresmore`，均通过。

**2026-08-27 实机反馈+回归修复**：用户反馈"刚转生开始，moonlightNight 下 common_house
依旧卡在 10 不动"。排查发现 `getCurrentStageIndex()` 复用的 `isBuildingUnlocked()` 把
"`req` 里只有 `type:'resource'`（建造花费），没有任何 `tech`/`building`/`prayer` 真实门槛"
的建筑也判定为"已解锁"——例如 `gan_eden`（age4，`req` 全是 food/faith/mana/supplies/cow
资源花费）、`research_plant`（age4，`req` 全是 gold/stone 资源花费），这类建筑从游戏一开局
（或刚转生）就满足"解锁"判定，导致 `getCurrentStageIndex()` 从第一回合起就把阶段 `S` 误判成
4。`common_house`（age1）因此 `gap=3`，`getStageCap` 算出 `80/2^3=10`——数字上跟 base
模板旧的 `common_house<10` 安全阀重合，容易误认为"老问题复发"，实际是新引擎自己把 target
钉死在了 10。

修复（`10-game-state-adapter.js`）：新增 `hasStageGate(building) = (building.req || [])
.some(req => req.type !== 'resource')`，`getCurrentStageIndex()` 的 `filter` 里加上
`hasStageGate(building) &&` 这一条——只有真正带科技/建筑/信仰门槛的建筑才计入阶段信号，
纯资源花费门槛（本质是"随时可造，只是贵"）不再误判为"已达到该阶段"。

**2026-08-27 第二次实机反馈+修复（`applyCapBridgeTargets`/`applyDangerousBattleBuildingTargets`
遗漏接入 `getStageCap`）**：用户反馈"common_house 依旧卡在10，且生产/制造类建筑停不下来，
粮仓却只建了1个"。排查发现：`applyCapBridgeTargets`（补仓储缺口）和
`applyDangerousBattleBuildingTargets`（危险战斗前军事建筑）这两个 `getTargets` 收尾阶段
调用的函数，在这轮"阶段驱动引擎"重构时**没有改用 `getStageCap`**，仍然用旧的
`building.cap || options.maxTarget`（默认 80）当上限，且用 `Math.max` 覆盖主循环已经算好的
阶段上限、固定给高优先级 9——任何命中"仓储缺口"或"危险战斗军事加成"条件的生产/制造类建筑，
会被这两个函数顶到 80 且持续触发，这才是"生产/制造类建筑不停建造"的真正原因（跟 common_house
的问题是两个独立 bug，同时暴露）。粮仓卡在 1 是另一个独立现象——粮仓走的是主循环的
`getProductionStorageCap`（90秒仓储溢出保护），不受这两个函数影响，本身是设计内行为（食物
仓储上限没提上去，多造粮仓也会溢出浪费），不是本次要修的对象。

修复：两个函数的 `cap` 计算都从 `building.cap || options.maxTarget || smartBuildDefaults.maxTarget`
改为 `getStageCap(building, options)`，`bridgeMax`/`battleMax` 的 `Math.min` 链也去掉了重复的
`maxTarget` 项（`getStageCap` 内部已经处理过）。已跑 `build.ps1` + `node --check` 验证通过。

**尚未解决、待用户决策的设计缺口**：`common_house`（age1，`living_quarters`，`gen` 里有
`{type:'population', id:'unemployed'}`）在 moonlightNight 路线/聚焦里**没有任何后继建筑
接盘**——`mansion`（age2，纯 `tech:architecture` 门槛，不是靠 `building:common_house` 数量
解锁）和 `house_workers`（`age:100`，游戏数据里天然标记为"不参与阶段过时"的常青建筑）都不在
moonlightNight 的 `buildingFocus`/`smartBuildRoutes` 里，一旦真实阶段 S 因为架构等科技研究
完成而涨到 4，`getStageCap` 会把 common_house 的目标正常衰减到 10（`80/2^3=10`，公式本身
没错），但没有任何建筑接手人口增长，导致人口容量被硬卡死。这跟最初讨论"基础资源类建筑要不要
区分对待"时验证过的 farm→granary→lucky_grove_b 链条不同——那条链条的后继建筑（granary,
`age:100`）虽然也没进路线表，但至少走通用打分循环能拿到 `resourceFocus` 加分部分弥补；
`mansion`/`house_workers` 对 moonlightNight 完全没有任何优先级来源。此问题不在本次两个 bug
修复范围内，需要用户决定：(a) 给"提供人口容量"的常青建筑（`gen.type==='population'` 或
`age:100`）加一条通用豁免规则不参与衰减；(b) 把 `house_workers`/`mansion` 补进各 goal 的
route/focus 表让它们自然接盘；(c) 干脆把 `living_quarters`/`resource` 这类"每个阶段都持续
需要"的建筑类别整体排除在阶段衰减之外（等于推翻此前"不区分，统一用阶段差值公式"的决定）。

**2026-08-27 第三次实机反馈+修复（结构性硬前置被阶段衰减压死——`getTargets` 上限计算完全
没有结构性下限通道）**：用户反馈"进入下一阶段的前提条件里有普通房屋到达15个，这里卡死了
后续全部流程"。排查发现：旧路线表里 `{id:'common_house', target:15}` 这个数字并不是随便定的
——`common_house:15` 是科技树里 **`municipal_administration`**（`req: building common_house
value:15`）和 **`end_ancient_era`**（`req: building common_house value:15` + `building
artisan_workshop value:5`）这两个科技的硬性建筑前置。这两个科技**不在**
`smartBuildGoals.moonlightNight.targetTechs`（只有 `architecture`/`establish_boundaries`/
`moonlight_night`）里，是通往这三个目标科技路上绕不开的中间前置科技（`moonlight_night`
自己的建筑前置其实是 `watchman_outpost:4`，已经在路线表里，不受影响）。本次"阶段驱动引擎"
重构把路线表里的硬编码数字全删了，却没有在 `getTargets` 的上限计算里接入任何"结构性硬前置"
下限通道——`getGoalRequirementBonus`/`getGoalFocusPrerequisiteBonus` 只影响打分（决定
`prio_${id}`），完全不影响 `max`（决定按钮上限），导致真实存在的硬性前置（15）被阶段衰减
（10）直接压死，游戏永远等不到能满足 `municipal_administration`/`end_ancient_era` 的那一刻，
后续科技全部卡死。这是本次重构遗漏实现 spec 里已声明的非目标（"buildingFocus 结构性前置
不受影响"），不是新的设计决策，直接修。

修复（`40-build-scoring.js`）：
- 新增 `getStructuralTechFloor(building)`：遍历**整个 `tech` 数组**（不局限于当前 goal 的
  `targetTechs`），对每个**尚未研究完成**（`!isTechCompleted`）的科技，找它 `req` 里
  `type:'building' && id===building.id` 的那一条，取 `req.value` 的最大值。故意做成
  **跨 goal 全局扫描**而不是"当前 goal 的 targetTechs"——因为卡住流程的往往是像
  `municipal_administration`/`end_ancient_era` 这种未被显式列为目标、但处于必经路径上的
  中间科技，事先无法穷举到 `targetTechs` 里；一旦某科技研究完成就从扫描里自动剔除，不会
  留下永久的虚高下限。
- 新增 `getStructuralFloor(building, goal)`：`Math.max(goal.buildingFocus 展开链的
  focusEntry.target, getStructuralTechFloor(building))`——把"结构性buildingFocus前置链"
  和"全局科技树硬性建筑前置"两个下限来源合并成一个值。
- `getTargets` 的 `max` 计算：把原来单独的 `getStageCap(building, options)` 项换成
  `Math.max(getStageCap(building, options), structuralFloor)`——阶段衰减只能在"没有真实
  硬前置"时生效，一旦某个数字是游戏规则写死要求的，阶段衰减不能把它压低于这个数字。
  `!prio && !routeTargetIds.has(...)` 的早退条件也加了 `&& !structuralFloor`，避免打分侧
  凑巧是 0 分但确实有结构性下限的建筑被提前跳过、连 `max` 都算不出来（实际验证过
  `getGoalRequirementBonus`/`getGoalFocusPrerequisiteBonus` 在这种情况下必然会把分拉满，
  这条早退分支理论上不会真的触发，纯防御性补全）。
- `getGoalTechs(goal)`（`20-goal-routes.js`）本身**不变**，继续只服务于打分侧
  `getGoalRequirementBonus`——"这个建筑对当前 goal 有多重要"和"这个数字是不是硬性下限，
  不能被压低"是两个独立问题，故意用两套扫描范围。

**跟"生产建筑造太多"投诉的关系**：用户同时反馈"其他生产建筑实在过多，最多几个到十个就够了"
——这条已经被第二轮修复（`applyCapBridgeTargets`/`applyDangerousBattleBuildingTargets` 接入
`getStageCap`）覆盖；新增的全局科技树下限是**有界的**（只会顶到某个未来科技真实要求的数字，
本次看到的实例都是个位数到15，不会顶到 `maxTarget=80`），不会重新引入"无限乱建"。

已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`（本轮三个修复合并跑的）。
**尚未实机验证**（房屋能否突破 10 并涨到 15、生产建筑数量是否收敛到合理区间，需要用户实机
确认）。

**不在本次范围**：base 模板 `common_house<10` 的硬编码安全阀本身未改动（永久业务逻辑，
需要单独确认才能碰）；base 模板"建筑优先级/上限"手动面板 UI 重做已与用户约定放到下一轮
单独的 brainstorming + spec。

**2026-08-27 第四次实机反馈+修复（真正根因：`getProductionStorageCap` 和结构性下限是
`Math.min` 链里的平级项，仓储保护会压过硬性下限）**：第三次修复后用户反馈"确认用了最新
版本，依然卡在10"。先排查了 base 模板的 `common_house<10` 硬编码安全阀（要求
`food.speed>1` 才允许继续建）——用户提供实机数据 `食物净增12.57/秒、总人口50、农民1`，
食物远超阈值，排除这条硬编码安全阀是本次的阻塞点。

改用**临时调试 console.log**（验证完已删除，不违反"临时调试代码可以直接加"的约定）直接
在 `getTargets` 里打印 common_house 每轮的 `count/cap/stageCap/structuralFloor/score/
prio/storageCap/max` 全量数据，拿到实机截图后一次定位：

```
goalId: "progress"（当前实际配置的目标其实是 progress，不是用户以为的 moonlightNight）
count: 10, cap: 80, stageCap: 40, structuralFloor: 15
storageCap: 10   ← 真正卡点
score: 206, prio: 7, willSkip: true
```

`structuralFloor`(15)和 `stageCap`(40)都算对了，`score`/`prio` 也很高——但
`getProductionStorageCap`（"90秒仓储填满保护"机制，2026-08-26 引入，见上方"存储上限驱动
的建筑数量规划"条目）单独算出了 `10`，而第三次修复只把 `structuralFloor` 塞进了
`Math.max(getStageCap(...), structuralFloor)` 这一项，`storageCap` 仍然和它是
`Math.min` 链里的平级项，谁小听谁的——`storageCap=10` 直接把整条链摁死在10，
`structuralFloor=15` 完全没能生效。

根因是之前没注意到 `common_house.gen` 除了 `food:-1`、`population:unemployed:1`，还带了
两个很小的副产出 `{gold:0.2}`、`{research:0.3}`（base 模板 302-349 行）——只要金币或研究点
仓库快满了（90秒填满阈值），`getProductionStorageCap` 就会把这类"顺带产出某资源"的建筑目标
钉死在当前数量。这套保护对纯生产建筑（比如灯塔多了会溢出金币）是对的，但普通房屋的核心作用
是提供人口/满足科技硬性前置，金币/研究只是无关紧要的副产出，不该被这个优化误伤——跟
`refugee_district` 那次"仓储保护压过结构性硬需求"是同一类问题（那次压到0，这次压在了
`count` 本身，表现不同但机制相同）。

**修复**（`40-build-scoring.js` 的 `getTargets`）：把结构性下限从 `Math.min` 链内部的
平级项，改成整条链算完之后的最终兜底——`stageCap`/`storageCap`/`maxExtra` 递增节流全部
降级为"软节流"（`softMax`），`structuralFloor` 单独在 `Math.max(softMax, Math.min(
structuralFloor, cap, maxTargetOption))` 里越过软节流生效（仍然被 `cap`/`maxTarget`
兜底，不会突破奇观单体上限或用户配置的总上限）：

```js
const softMax = Math.min(
  cap,
  maxTargetOption,
  count + Math.min(Number(options.maxExtra) || smartBuildDefaults.maxExtra, toExtra(score)),
  getProductionStorageCap(building, resourceMap, options),
  getStageCap(building, options)
);
const max = Math.max(softMax, Math.min(structuralFloor, cap, maxTargetOption));
```

**顺带发现，尚待用户确认**：调试日志里 `goalId: "progress"`——用户以为自己在跑
moonlightNight，但脚本面板里实际配置的目标是 `progress`。这次的 storageCap bug 跟目标
无关（哪个 goal 下都会触发），但如果用户确实想跑 moonlightNight 路线，需要自己去脚本面板
把目标切换过来，这不是代码能自动纠正的部分。

已跑 `build.ps1` + `node --check`，均通过；调试 `console.log` 已在验证后从
`40-build-scoring.js` 中删除，构建产物不含调试输出。**尚未实机验证**（本轮修复后房屋能否
真正涨过10并到15，需要用户重新加载脚本后确认）。

**用户已实机验证通过**："目前可以到达15进入下一层了"——`structuralFloor` 越过软节流链的
修复生效。

### 2026-08-27（第五次）：彻底移除"仓储填满保护"（`getProductionStorageCap`）——溢出在这个游戏里不是问题

用户看到上面的修复生效后，进一步反馈：`getProductionStorageCap` 这套"临近溢出才把目标钉死"
的被动机制本身就不该存在——原话："不需要溢出保护，当前就是有很多种类的资源会溢出，我们需要
的是一开始就根据当前情况计算出它应该要的上限"；追问具体哪些资源溢出时进一步澄清："这就是
常见的正常情况，不应该因为这个额外处理……不存在溢出浪费导致的问题，这里溢出就是正常情况不要
纠缠"。

结论：这不是调参能解决的，是这套机制的存在前提（"溢出=浪费，需要提前刹车"）本身不成立。
经 brainstorming 确认后（用户选择"整体删除"而非"降级为软性打分惩罚"），产出 spec
`docs/superpowers/specs/2026-08-27-remove-production-storage-cap-design.md`（commit
`3ef2970`），并已实现：

- `40-build-scoring.js`：删除 `PRODUCTION_STORAGE_CAP_SECONDS` 常量和 `getProductionStorageCap`
  函数整体；`getTargets` 的 `softMax` 计算链去掉这一项，只剩
  `Math.min(cap, maxTargetOption, count + min(maxExtra, toExtra(score)), getStageCap(...))`。
- 影响：`granary` 等此前"卡在1个"的建筑（`.codemap-conventions.md` 第二次实机反馈条目里记录
  为"设计内行为、非bug"）会转为正常受阶段衰减/目标信号驱动增长；`refugee_district` 那次
  `Math.min(cap, Math.max(getCount(building), 1))` 的半修复随函数整体删除一起消失（正确，
  问题根源函数已不存在，不需要保留死代码）。
- 2026-08-26 spec（`docs/superpowers/specs/2026-08-26-storage-driven-building-cap-design.md`）
  的"①生产类建筑数量上限——边际填满时间阈值法"一节自本次起废弃；该 spec 其余部分（泰坦覆盖、
  `applyCapBridgeTargets` 存储缺口候选建筑过滤、手动配置兜底）不受影响，继续保留。

已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`，均通过。**尚未实机
验证**。

### 2026-08-27（第六次）：危险科技安全阀按 goal 开例外——moonlightNight 目标下"月明之夜"研究不出

背景：`moonlight_night` 科技通过 `smartBuildDangerousFights`（`00-data-tables.js`）映射到战斗
`army_of_goblin`；2026-08-26 第四次修复把 `shouldGateDangerousResearch` 的安全校验从"只在
`armyEnabled` 开启时生效"改成了"对所有目标无条件生效"（防止 `dragon_assault` 那样的暴毙重演）。

用户反馈：选择 `moonlightNight` 目标时，"月明之夜"一直不被自动研究。排查（`base` 模板
47935-47963 行的研究点击循环）确认：这个安全阀现在无差别拦住了所有目标，而 `moonlightNight`
是唯一一个以这项科技本身命名、把它当作核心目标（而不是顺路科技）的 goal——用户原话确认该目标
下"就应该直接跳过安全检查"，并给出更通用的原则："每个目标应该直接对应一套配置"。

`moonlight_night` 同时也是几乎所有其他 goal（`progress`/`wonderRush`/`religionGrowth`/
`richPath`/`lategame`，见 `00-data-tables.js` 57-112 行）`targetTechs` 里的过渡科技，这些
目标下应继续保留安全校验，避免重演 2026-08-26 那次暴毙。

经 brainstorming 确认后，产出 spec
`docs/superpowers/specs/2026-08-27-goal-scoped-dangerous-research-override-design.md`
（commit `4725ee7`），并已实现：

- `00-data-tables.js`：给 `moonlightNight` goal 新增字段
  `dangerousResearchOverrides: ['moonlight_night']`；其余 goal 不填（默认 `undefined`，
  安全阀行为不变）。
- `30-dangerous-fight-gate.js`：`shouldGateDangerousResearch` 新增一步——
  `const goal = getGoal(options); if ((goal.dangerousResearchOverrides || []).includes(researchKey)) return false;`
  ——命中当前 goal 的例外列表就直接放行，不再要求 `canWinBattle` 先通过。
- 不影响 `getBlockedDangerousFights`/`getNextDangerousFight`（驱动建筑/兵种备战打分加成的
  另一套逻辑，跟"要不要点研究"是两个独立判断点，本次未改动）。

已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`，均通过。**尚未实机
验证**（需要确认 moonlightNight 目标下"月明之夜"能被点击研究、且其他目标下危险科技安全阀
依然正常拦截）。

### 2026-08-27（第七次）：彻底去掉打分驱动的通用建筑数量上限——目标优先，不再纠缠不必要的生产建筑

背景：用户反馈选定 moonlightNight 目标后依然会造大量非必要生产/制造建筑（举例"20个采石场"），
认为既然选了目标就应该以尽快达成目标为唯一优先级，而不是被通用打分机制牵着走去堆生产建筑。

排查发现根因：`getTargets` 里建筑数量上限实际来自两条独立路径——
①结构性/目标信号驱动的 `structuralFloor`（科技树硬性数量要求 + `buildingFocus`/route 的
显式目标，此前默认 `target:1`），②打分驱动的通用 `softMax`（`count + toExtra(score)` 结合
`getStageCap`，`getStageCap` 对处于当前时代或更领先时代的建筑几乎不设实质上限，能一路涨到
`maxTarget`≈80）。`quarry` 虽然在 `moonlightNight` 的 `buildingFocus` 里，但那只贡献了
`target:1` 的**下限**，真正的**上限**始终来自②这条打分链，导致"在焦点列表里"完全不能限制
数量——这是"20个采石场"的真正原因，不是打分权重没调好。

用户在追问过程中明确否决了两版基于实时信号的修复方向：
1. 第一版提议（按当前资源缺口动态封顶生产建筑）被否：
   "我一开始就提过，你一直纠缠别的不处理这个，我要求的是读取当前转生，超转生，继承的状态，
   根据这个结果来生成各个具体建筑在各个目标中的上限"。
2. 第二版提议（改用实时资源净增速 `res.speed` 判断是否还需要建）被更明确地否：
   "不要再纠缠现有的资源速率来判断了……一切的规范方向都是达到目标"。

最终收敛为纯结构化设计（经 brainstorming 确认，用户回复"确认"）：
**建筑数量上限只由两类结构性信号决定，完全砍掉打分驱动的通用上限**——
`floor = Math.max(structuralTechFloor, focusOrRouteTarget)`；不在结构性硬需求/route/
buildingFocus 里的建筑，数量维持在当前值不再自动增长（除非被下面提到的存量缺口桥接机制
命中）；`scoreBuilding`/`toPriority` 保留但只决定同批候选建筑的点击顺序，与"造多少个"完全
解耦。产出 spec `docs/superpowers/specs/2026-08-27-goal-driven-build-ceiling-design.md`
（commit `7bbc11e`），并已实现：

- `40-build-scoring.js`：删除 `getStructuralFloor`；新增
  `getFocusOrRouteTarget(building, goal, route)` = `Math.max(focusEntry.target || 0,
  routeEntry.target || 0)`（`focusEntry` 来自 `getExpandedGoalFocusTargets(goal)`，
  `routeEntry` 来自 `getRouteEntry(building, route)`，两者都已在 `expandPrerequisiteTargets`
  里展开过前置建筑链）；`getTargets` 主循环改为
  `floor = Math.max(getStructuralTechFloor(building), getFocusOrRouteTarget(building, goal,
  route))`，`floor` 为 0 直接跳过该建筑，否则
  `max = Math.min(Math.max(floor, count), cap, maxTargetOption)`——不再有独立的 `softMax`/
  `toExtra`/`getStageCap` 参与"造多少个"的计算。
- `getStageCap`/`toExtra` 函数本身未删除，仍被 `applyCapBridgeTargets`/
  `applyDangerousBattleBuildingTargets`（存量缺口桥接、备战建筑加造，这两条是"结构性缺口触发
  才建"的合法增长路径，本次未改动）和 `60-research-scoring.js`/`80-prayer-scoring.js` 的科技/
  祈祷打分沿用。
- `getRouteEntry`/`getExpandedRouteTargets`/`getExpandedGoalFocusTargets`（`20-goal-routes.js`）
  均为只读引用，未改动。
- 已知后续可能需要跟进：`moonlightNight` 的 `buildingFocus`（`quarry`/`artisan_workshop`）
  目前仍是默认 `target:1`，如果实机验证发现1个不够用，修法是在 `00-data-tables.js` 里给这些
  条目手工加大 `target` 数值（数据调整，不是逻辑改动），不要在验证前预先加。

已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`（18 files, 549
symbols），均通过。

**用户实机验证反馈（暴露两个新问题，见下面第八次条目）**："1.无关的研究也没必要点；
2.在完成了4个守望者前哨后没有自动点击月明之夜"——建筑数量上限本身的重构没有被推翻，
是研究选择这一侧还留着同类问题，以及第六次那个 override 有个 `||` 短路漏洞没堵上。

### 2026-08-27（第八次）：研究选择改为结构性信号门槛驱动 + 修复 moonlightNight override 被 `||` 短路

背景：验证第七次建筑侧改动时，用户反馈两个新问题（见上）。

**问题1排查**：`60-research-scoring.js` 的 `scoreResearch` 给几乎所有非危险科技一个固定基础分8
（危险科技4），只要不在 `resetResearch`/`researchExcludes` 里就有非零优先级，导致跟目标无关的
科技也会被点——跟建筑重构前"打分决定造不造"是同一类问题的研究版本。

**问题2排查**：`moonlight_night` 科技的结构性前置正是
`{type:'building', id:'watchman_outpost', value:4}`（游戏源码 `moonlight_night` 定义），用户已
满足；但 base 模板研究点击循环（47935行）的门禁是
`state.options...dangerousFights || smartBuildPlanner.shouldGateDangerousResearch(research.key)`
——`||` 短路：只要游戏自带的手动开关是开的，即使我们的 `shouldGateDangerousResearch` 因
`dangerousResearchOverrides` 命中而返回 `false`，整体判断依然是 `true`，第六次的 override 实际
没生效。

经 brainstorming 确认（用户对两个问题分别用 AskUserQuestion 选择"按结构性信号门槛设计"和
"同意修改 base 这一行"），产出 spec
`docs/superpowers/specs/2026-08-27-goal-driven-research-gate-design.md`（commit `ca77716`），
并已实现：

- `60-research-scoring.js`：新增 `isDirectlyRelevantResearch(technology, options, goal, route)`
  = `targetTechs` 包含该科技 `|| getTechUnlockBonus(...) > 0 || getPrayerTechBonus(...) > 0`；
  新增 `expandTechPrerequisites(seedIds)`（写法照抄 `20-goal-routes.js` 的
  `expandPrerequisiteTargets`，只是把"建筑数量前置"换成"科技前置"——沿 `tech.req` 里
  `type==='tech'` 的依赖递归展开，防环用 `visiting` 标记，已完成科技跳过；数据已用真实科技定义
  验证：`daylong_celebration` 的 `req` 含 `{type:'tech', id:'moonlight_night'}`）。
  `getResearchTargets` 先用 `isDirectlyRelevantResearch` 筛出"直接相关"种子集合，
  `expandTechPrerequisites` 递归展开这些种子各自的前置科技链，只有落在"直接相关 ∪ 前置链"里的
  科技才走原有 `scoreResearch` 打分给非零优先级，其余一律0——`scoreResearch`/`getResearchGroups`/
  `applyResearchManualOverrides` 本身未改动，继续只负责"已过门槛的候选怎么排序"。
- `30-dangerous-fight-gate.js`：新增 `isDangerousResearchOverridden(researchKey)`——跟
  `shouldGateDangerousResearch` 共用同一个 `dangerousResearchOverrides` 数据，单纯返回"当前 goal
  是否对这个科技开了例外"这个布尔值，供 base 模板短路游戏自带开关用。
- `automation-src/base/Theresmore-Automation_4.14.4.base.user.js` 第47935行：门禁表达式改写为
  `smartBuildPlanner.isDangerousResearchOverridden(research.key) ? false : (游戏自带开关 ||
  shouldGateDangerousResearch(...))`——命中 override 时无视游戏自带开关直接放行到后续
  `canWinBattle`/`canWinNow` 判断；未命中时行为跟改动前完全一致。**这是本次唯一一处 base 模板
  改动，经用户针对这一具体改动单独确认过，不代表放开"允许直接改 base 模板"的一般性授权。**

已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`（18 files, 552
symbols），均通过。**尚未实机验证**——需要确认：`watchman_outpost` 满4个后 `moonlight_night`
能否被点击研究（仍受 `canWinBattle` 军队实力限制，打不过会继续等，属预期）；其他目标下危险科技
安全阀是否仍正常拦截；跟当前目标无关的科技是否不再被点；目标链条上的科技（含前置链）是否仍能
正常点完不卡住。

### 2026-08-27（第九次）：原料产出建筑数量兜底——数据修正 + 通用资源生产速率桥接

背景：验证第八次改动前，用户又提出第三个问题（以 `moonlightNight` 为例）：目标建筑
`watchman_outpost`（结构性 floor=4）本身要消耗原料才能造，而原料产出建筑（"木匠工坊"/
"工匠公会"）当前卡在 `buildingFocus`/route 默认 `target:1`，达标后 spec3 的新 floor 逻辑
不会再继续扩建它们，导致原料产能跟不上目标建筑的建造需求。

直接查证（`Read` base 文件）：`watchman_outpost` 的 `req` 是 `wood 2000`/`tools 1000`/
`supplies 100`/`crystal 70`（均 `multi 1.4`）。`guild_of_craftsmen`（"工匠公会"）是**直接
资源产出型**（`gen` 里有 `type:'resource'` 的 `building_material`/`steel`/`crystal`/
`supplies` 平铺产出，直接对应 `watchman_outpost` 要的 `crystal`/`supplies`）；
`carpenter_workshop`（"木匠工坊"）和 `artisan_workshop` 都是**岗位坑位型**（`gen` 只有
`type:'population'` 坑位 + 针对该岗位的百分比 `modifier`，岗位基础产出速率在游戏引擎岗位表里，
不在本项目 `buildings` 数据表中，拿不到精确产出速率）——`watchman_outpost` 要的 `tools` 就是
靠 `artisan` 岗位基础产出，`artisan_workshop` 只提供坑位。

经 brainstorming 确认（用户对"该往哪个方向修"选择"A+B：先救急再建通用能力"），产出 spec
`docs/superpowers/specs/2026-08-27-goal-driven-resource-production-bridge-design.md`
（commit `b37a12e`），并已实现：

- `00-data-tables.js`：`smartBuildRoutes.moonlightNight.buildingTargets` 的 `artisan_workshop`
  条目、`supportTargets` 的 `carpenter_workshop` 条目都加上 `target: 3`——这两个是岗位坑位型，
  无法被下面的自动机制识别，只能手工数据兜底；`3` 是经验起点，不是精算值，跟第七次遗留的
  `quarry`/`artisan_workshop` 默认值一样，留待实机验证后再调。
- `40-build-scoring.js`：新增 `registerResourceProductionShortfall(shortfalls, req,
  resourceMap, source, options)`（判据：`getResourceCost` 算出的成本超过当前存量 `res.current`，
  且按 `res.speed` 净生产速率补上缺口的等待时间超过 `options.maxWaitSeconds`，才记为缺口——复用
  `getCostWait`/`scoreBuilding` 已经在用的同一套阈值，不新增配置项）；新增
  `getResourceProductionShortfalls(goal, route, resourceMap, options)`（需求来源是
  `getGoalTechs`/`getExpandedRouteTargets`/`getExpandedGoalFocusTargets`，跟已有的
  `getResourceCapShortfalls` 系列同一批函数，只是把"cap 够不够存"的判据换成"speed 够不够快"）；
  新增 `applyProductionBridgeTargets(targets, subpage, resourceMap, options)`（对
  `gen.type==='resource' && value>0` 的直接产出型建筑，命中缺口就用跟 `applyCapBridgeTargets`
  相同的 `count + maxExtra`（不超 `getStageCap`）渐进式桥接，`prio_${id}` 提到9）；接入点在
  `getTargets` 里紧跟 `applyCapBridgeTargets` 之后加一行 `applyProductionBridgeTargets(...)`。
- 明确的覆盖盲区：岗位坑位型产出建筑（如 `carpenter_workshop`/`artisan_workshop`）不会被
  `applyProductionBridgeTargets` 识别到（判据是 `gen.type==='resource'`，它们没有这类条目），
  这类建筑的数量兜底永远要靠第一条的数据修正手工做，本次未打算、也做不到让它们自动化。
- 未改动结构性 floor 主循环（`getStructuralTechFloor`/`getFocusOrRouteTarget`/`getTargets`
  主循环判断部分）、`applyCapBridgeTargets`/`applyDangerousBattleBuildingTargets`（同层级独立
  桥接，互不干扰）、base 模板（本次全部改动在 fragments 层，未碰 base）。

已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`（18 files, 555
symbols），均通过。**尚未实机验证**——需要确认：`moonlightNight` 下 `artisan_workshop`/
`carpenter_workshop` 能否建到3个（而非卡在1个）；`guild_of_craftsmen` 在 `crystal`/`supplies`
产能不足时能否被自动追加建造目标；`watchman_outpost` 建满4个的整体耗时是否比之前明显缩短；
其他目标下 `applyProductionBridgeTargets` 是否只在真正出现资源速率缺口时才触发，没有误干扰
正常建筑目标。

### 2026-08-28：生产速率桥接精算化（spec8，commit 待定）

延续 spec5（上一节）。用户诉求"以实现目标的路径为主，自动计算各个建筑上限"，不满足于
`target: 3` 这种经验值。brainstorming 确认范围后产出 spec
`docs/superpowers/specs/2026-08-28-precise-production-bridge-sizing-design.md`，并已实现：

- **关键发现**：`carpenter_workshop`/`artisan_workshop`/`quarry` 这类"岗位坑位型"建筑缺的
  岗位基础产出速率数据，其实 **base 模板本来就有**——
  `automation-src/base/Theresmore-Automation_4.14.4.base.user.js:18941` `var jobs = [...]`
  是一份完整的岗位表（`{id, req:[{type:'building',...}], gen:[{type:'resource',...}]}`），
  跟 `buildings`（302行）/`tech`（33038行）/`units`（42037行）一样是顶层全局引用数据，
  fragments 可以直接引用 `jobs.find(...)`，不需要新建数据表、也不需要从
  `files/theresmore/main.js`（用户提供的网页游戏源码抓取，第56341-56495行 `Be` 数组是同一份
  数据的压缩版）另外抄一份。**spec5 遗留的"岗位坑位型产出建筑无法自动识别"覆盖盲区到这里
  已经解决。**
- `40-build-scoring.js`：新增 `getJobForBuilding(building)`（从 `building.gen` 的
  `{type:'population', id}` 坑位反查 `jobs` 表同 id 的岗位定义）、
  `getBuildingResourceRate(building, resourceId)`（统一"直接资源型"`building.gen`和"岗位
  坑位型"`job.gen`两种取速率路径，返回值可能为负=净消耗）；重写 `applyProductionBridgeTargets`
  （原渐进式 `count+maxExtra`，改成用 `deficit/maxWaitSeconds` 反解精确还需新增数量：
  `requiredSpeed - shortfall.speed` 除以 `perUnitRate` 再向上取整，多个可缓解的缺口取
  最大需求量）。`getResourceProductionShortfalls`/`registerResourceProductionShortfall`
  不变（缺口识别逻辑没动，只改"桥接到多少"的算法）。
- `00-data-tables.js`：`moonlightNight` 的 `artisan_workshop`（buildingTargets）、
  `carpenter_workshop`（supportTargets）两条目去掉手工 `target: 3`，改由精算机制决定该建几个，
  跟 `guild_of_craftsmen` 待遇对齐。
- 明确的精度取舍（用户已确认接受）：不叠加建筑/科技对岗位的百分比 `modifier`（如 `quarry`
  自带对 `quarryman` 的 +100% 石头加成），精算结果偏保守（可能多建一点，不会少建）；不处理
  "多建产出建筑增加原料消耗、诱发新缺口"的连锁效应（交给既有机制下一轮自然接住）。
- 已跑 `build.ps1` + `node --check` + `codemap regen --repo theresmore`（18 files, 557
  symbols），均通过。**尚未实机验证**——需要确认精算结果是否合理（不过度保守、也不再卡在1个）、
  `guild_of_craftsmen` 等直接资源型建筑收敛速度是否比渐进式更快。

### 游戏源码参考：`files/theresmore/main.js` / `chunk.js` / `chunk2.js`

用户从游戏网页 F12 Sources 导出的**游戏本体源码**（webpack 打包产物，`main.js` 约 2.8MB，
压缩但未混淆变量名），不属于本项目自动化脚本本身，也未注册为 codemap repo。
**以后再遇到"游戏内部状态结构不透明/读不对"类问题（比如 `idxs`/`run` 某个字段到底存的是
什么、某个 Store 的方法怎么更新状态），优先来这份源码里搜索真实实现，不要靠
console.log 在游戏里反复试错猜测**——`main.js` 2.8MB 太大不能整体 Read，用 `Grep`
（带 `head_limit`）搜目标字符串（建筑/科技/单位 id、`idxs.xxx`、Store 类名等）定位行号后
再 `Read` 配 `offset`/`limit` 精读上下文，和处理 base 模板巨型脚本的方式一样。

## automation-src/fragments/smart-build-options.js
单个 `smartBuild` 默认配置对象（约 17 行），字段不多，直接读全文比查符号快，不单独维护索引。

## automation-src/fragments/smart-build-panel.template.html
smartBuild 自身开关面板（56 行），注入到 base 模板第 50069 行附近（Cheats 页签内）。

## automation-src/build.ps1
2026-08-26 改为读取 `fragments/smart-build-planner/` 目录下文件（新增 `80-prayer-scoring.js`
后共 10 个）并按固定顺序清单拼接
（`00-data-tables.js` 在外部，`10~90` 拼接后包进 `const smartBuildPlanner = (() => {...})();`），
取代原来单文件直读。改动 `automation-src/fragments/smart-build-planner/` 下任何文件后仍然是
跑 `automation-src/build.ps1` 重新生成产物 + `node --check` 校验语法。

## automation-src/base/*.base.user.js（5 万+ 行单体模板，构建产物外壳）
不是日常维护目标，AGENTS.md 明确禁止手改永久业务逻辑（临时调试代码不受此约束，见
项目根目录 CLAUDE.md"维护工作流"一节，验证完必须删除）。已确认的关键锚点（新增子系统时
来这里插一行调用 planner，不要在附近大改结构）：

- Build 建筑注入点：`const buildingsObject = smartBuildPlanner.getTargets(subpage, configuredBuildingsObject) || configuredBuildingsObject;` — 约 47312–47313 行（拆分重构后行号已整体下移，见下方"重构后已知的行号漂移"）
- Build 手动配置 UI（大列表，8 类）：模板字符串约 49482–49529 行；smartBuild 开启且未勾
  manualOverrides 时完全不生效，仅作 fallback / manualOverrides 输入源
- Build 建筑列表构建/点击执行 `getBuildingsList()`/`getAllButtons()`：约 47311–47358 行
- Army 兵种数据读取点：约 46870–46871 行；Army 兵种手动配置 UI：约 49670–49775 行
  （`getUnitTargets` 只有在存在被阻挡的危险科技战斗时才接管，多数时间手动列表仍生效）
- Army Explore 自动化注入点 `executeAction$7`（2026-08-26 新增，已获用户同意）：约
  47016–47032 行，`const exploreOptions = smartBuildPlanner.getExploreTargets(configuredExploreOptions) || configuredExploreOptions;`，与 Build/Army/Research 注入点同款写法；
  Attack（战斗自动勾选）自动化仍未接入，明确延后到后续阶段。
- Research 候选科技读取点 `getAllowedResearch()`：约 47879–47899 行，`const researchOptions = smartBuildPlanner.getResearchTargets(configuredResearchOptions) || configuredResearchOptions;`——
  这是本项目 CLAUDE.md"禁止手改 base 模板"规则生效后第一次经用户明确同意的例外，Explore
  注入点是第二次；后续新增子系统若要同样接入 base 模板，仍需先向用户确认；
  Research 手动配置 UI（Exclusive/Regular/Dangerous 三区块）：约 49530–49595 行；
  `resetResearch`（终局/不可逆科技，约 47875 行）、`unsafeResearch`（危险科技，约 49383 行）、
  `groupChoices`（互斥科技分组函数，约 46835 行）这三个 base 脚本里的既有变量/函数被
  smart-build-planner 直接按名引用（同一顶层作用域，只在游戏循环调用时才求值）
- Research 执行/点击循环 `executeAction$3()`（含终局科技 `resetResearch` 特殊处理）：约
  47860–48053 行区间
- Magic/Prayers 候选祈祷读取点 `getAllowedPrayers()`（2026-08-26 新增，已获用户同意，
  第三次例外）：产物里约 49096 行，
  `const configuredPrayersOptions = state.options.pages[CONSTANTS.PAGES.MAGIC].subpages[CONSTANTS.SUBPAGES.PRAYERS].options; const prayersOptions = smartBuildPlanner.getPrayerTargets(configuredPrayersOptions) || configuredPrayersOptions;`
  ——与 Research/Explore 注入点同款写法。`getAllButtons$1`/`executeAction$2`/
  `userEnabled$2`/`hasMagics()` 均未改动，沿用既有 DOM 按钮存在性二次过滤（未解锁的
  候选祈祷自动被滤掉，规划器不需要自己判断解锁状态）。Magic/Spells 子页面（法力系统，
  `userEnabled$1`/`executeAction$1`）未接入，超出本次范围。
- Population 职业列表：49636–49650 行；Diplomacy：49876–49912 行；
  Prestige：49996–50013 行 —— 均未接入 planner，均是未来待做子系统

**重构后已知的行号漂移**：上面几条"约 XX 行"的锚点是拆分前（单文件版
smart-build-planner.js）生成产物里的行号，拆分为 9 个文件后拼接方式变了（多了文件间的
换行拼接），生成产物里 base 模板部分的绝对行号会整体下移一些（下移量 = 新增的换行数，
数量很小，通常几行以内）。**这些锚点仍然可靠，只是行号可能有个位数到十位数的偏差，
定位时先按行号 ±20 行搜索，不必因为行号对不上就怀疑锚点失效。** 下次大改动 base 模板
锚点周边代码后，应该重新核实一次精确行号并更新本文件。

维护完这份文件后跑一次 `codemap regen --repo theresmore` 确认合并进 CODEMAP.md。
